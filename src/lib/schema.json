{
  "$schema": "http://json-schema.org/schema",
  "id": "PlatformAngularLibrary",
  "title": "Platform Angular Library Schema",
  "type": "object",
  "description": "Generates a new Angular library in the \"client \" subfolder of the workspace.",
  "properties": {
    "projectRoot": {
      "description": "The root directory of the new library.",
      "type": "string",
      "visible": false
    },
    "name": {
      "description": "The name of the new library.",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the library?"
    },
    "skipInstall": {
      "description": "Whether or not to install node packages.",
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "name"
  ]
}
